# Simplified Team Narratives

- These files are distilled from `team_wise_data/` for presenter-led deck building.
- They are intentionally shorter than the source markdown.
- Keep the schema markers visible in slides: `Activity`, `Output`, `Outcome`, `Impact`.
- Use these files as slide-copy source, not as final layout instructions.
